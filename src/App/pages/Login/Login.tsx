import { ReactElement } from "react";
import { StyledContainer } from "../../../assets/ui/global";

function Login(): ReactElement {
    return (
        <StyledContainer>

        </StyledContainer>
    );
}

export default Login;